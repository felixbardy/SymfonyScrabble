{% extends 'base.html.twig' %}

{% block title %}Scrabble!{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-6">
            <div class="row  card shadow border rounded-3 p-4">
                <!---->
                <div class="row">
                    <div class="col-12">
                        <h3>Voilà tes lettres</h3>
                        <table class="table">
                        <tbody>
                        <tr>
                        {% for letter in input %}
                            <td class="text-center m-5">
                                <h5>
                                    {{ letter|upper }}
                                </h5>
                            </td>
                        {% endfor %}
                        </tr>
                        </tbody>
                        </table>
                    </div>
                </div>
                <div class="row">
                    <div class="col-12">
                        <h4>Trouves les mots avec le score le plus haut!</h4>
                        {{ form(submit_word_form) }}
                    </div>
                </div>
            </div>
            <div class="row  card shadow border rounded-3 p-4">
                <h3>Tentatives:</h3>
            </div>
        </div>
        <div class="col-6">
            <h2>Meilleurs mots d'hier:</h2>
        </div>
    </div>
{% endblock %}